# Example: ESP32 with individual GPIO buttons (7 GPIOs for 7 buttons)
#
# Hardware setup:
#   - Each button connects a GPIO to 3.3V through a momentary switch
#   - Internal pulldowns are used
#   - See buttons_menu_gpio.yaml for button mapping
#
# This uses more GPIOs than the ADC ladder approach but is simpler to wire.

substitutions:
  name: 'coder-gpio'
  # Outputs
  gpio_code: 'GPIO16'
  gpio_led: 'GPIO23'
  gpio_led_invert: 'false'
  gpio_reverse: 'GPIO13'
  gpio_shunt: 'GPIO17'
  gpio_trigger: 'GPIO32'
  # Inputs
  gpio_zone: 'GPIO33'
  gpio_zone_invert: 'false'
  gpio_presignal: 'GPIO34'
  gpio_presignal_invert: 'false'
  # I2C (for OLED)
  gpio_scl: 'GPIO25'
  gpio_sda: 'GPIO27'
  # Panel buttons (menu layout)
  gpio_toggle: 'GPIO19'
  gpio_menu_up: 'GPIO21'
  gpio_menu_down: 'GPIO18'
  gpio_type_up: 'GPIO4'
  gpio_type_down: 'GPIO26'
  gpio_select_up: 'GPIO14'
  gpio_select_down: 'GPIO22'
  # Pulse counter (for reading incoming codes)
  #gpio_code_reader: 'GPIO36'

packages:
  main: !include main.yaml
  buttons: !include includes/buttons_menu_gpio.yaml
  display: !include includes/display_oled.yaml
  diagnostics: !include includes/diagnostics.yaml
  #pulse_counter: !include includes/pulse_counter.yaml
